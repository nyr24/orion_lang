{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/debug/app",
            "args": ["../../res/test.ori"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/bin/debug/",
        }
    ]
}
